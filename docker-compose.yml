services:
  db:
    image: postgres:16
    container_name: yash_poultry_db
    environment:
      POSTGRES_DB: yash_poultry
      POSTGRES_USER: yash
      POSTGRES_PASSWORD: yash_dev_password
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
